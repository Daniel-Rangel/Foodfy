{% extends "layout.njk" %}

{% block content %}
    <section class="showcase">
      <article class="showcase__description">
        <h1 class="showcase__title">As melhores receitas</h1>
        <p class="showcase__text">
          Aprenda a construir os melhores pratos
          com receitas criadas por profissionais
          do mundo inteiro.
        </p>
      </article>
      <div class="showcase__container-img">
        <img src="/img/chef.png" alt="chef" class="showcase__img">
      </div>
    </section>

    <main class="revenues">
      <h1 class="revenues__accessed">Mais acessadas</h1>
      <section class="lists">
        {% for food in foods %}
          <article class="list__item">
            <div class="list__figure">
              <img src="{{food.image}}" alt="" class="list__img">
            </div>
            <h1 class="list__title">{{food.title}}</h1>
            <p class="list__cooker">por {{food.author}}</p>
          </article>
        {% endfor %}
      </section>
    </main>
{% endblock content %}